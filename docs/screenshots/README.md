# Screenshots

Drop your images in this folder, then reference them from the main
[`README.md`](../../README.md).

Suggested shots to capture:

| Filename (suggested) | What to capture |
|----------------------|-----------------|
| `enrollment-hero.png` | The email input prompt the end user sees |
| `success-dialog.png`  | The "enrollment completed successfully" dialog |
| `result-requires-action.png` | A "requires action" / blocked-device message |
| `device-record.png`   | The JumpCloud device record showing the bound user / diagnostics |

To show one in the main README, replace the placeholder comment near the top with:

```markdown
![Device enrollment in action](docs/screenshots/enrollment-hero.png)
```
